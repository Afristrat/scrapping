import { describe, it } from 'vitest'

// Monitoring page is now a redirect to /costs.
// Tests for cost display live in Costs.test.tsx.
describe('Monitoring (redirect)', () => {
  it('placeholder - see Costs.test.tsx', () => {
    // No-op: Monitoring page removed in favor of Costs.
  })
})
